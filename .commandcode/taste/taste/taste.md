# Taste
- Wants the agent to read all project documentation completely before planning — then discuss together before writing code. Prefers a "read docs → discuss → create spec → implement" flow over jumping straight to code. Confidence: 0.7
- Communicates in brief directives and defers technical execution to the agent once direction is agreed — answered multiple-choice stack decisions quickly, then let the agent run the full build with minimal input, using terse "continue" prompts to resume stalled work rather than re-explaining. Confidence: 0.5
- Prefers free-tier / no-cost APIs over paid ones when given the choice — picked NVIDIA NIM free models (OpenAI-compatible) over the recommended paid Anthropic setup, plus a free-tier search API (Exa). Cost-conscious when selecting providers. Confidence: 0.6
- Prefers building new features inside the existing repo/monorepo scaffold, reusing its workspace packages and tooling, rather than spinning up standalone folders or fresh repos. Confidence: 0.6
- When offered scope choices on optional features, leans toward the option that makes internal behavior observable — chose a live streaming decision UI over CLI-only logging for an agent project. Confidence: 0.5
- Expects resilience built in by default for networked/agent code — flagged its absence as a defect when a run stalled mid-research ("why don't we have some retry mechanism"). Wants timeouts on every external call, retries with backoff, per-item failure isolation, and graceful degradation/fallback output instead of fail-fast crashes. Confidence: 0.8
- Debugging process: root-cause-first, no speculative fixes. When a bug persists, wants instrumentation (console logs) added and the exact failing case reproduced to confirm the diagnosis "to be 100% sure before making the code changes" — pushed back after a fix was applied on hypothesis without reproducing the real failure. Confidence: 0.9
